Aria.interfaceDefinition({
	$classpath: 'model.ui.templates.common.ITakeNoteCtrl',
	$extends: 'model.ui.controller.IApplicationCtrl',
	$interface: {
		/**
		 * fetches header data stored in controller
		 */
		saveNoteData: function(args) {}
	}	
});